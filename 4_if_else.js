

// If else in js

let age=20;
if(age<=17){
    console.log(`Your age is `+age+`.You can't buy this ticket. `);
}

else if(age>=18 && age<=25){
    console.log(`Your age is `+age+`.You can buy this ticket.`);
}
else{
    console.log(`you are is `+age+`.You can't buy this ticket`);
}